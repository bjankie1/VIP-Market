package utils

/**
 * Renders HTML based on Wiki markup
 */
object HtmlRenderer {

  /**
   * Replaces WIKI markup with corresponding html elements:
   * <ul>
   * <li>{{img id="1"}} with corresponding img tag pointing to given image uploaded
   * </ul>
   */
  def render(s: String) = {
    s
  }
}